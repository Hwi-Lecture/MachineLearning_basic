# AI 활용 실습 노트북 작성 원칙

학습자가 AI에게 코드를 요청하고 실행 결과에서 다음 탐색을 이어가는 실습용 Jupyter 노트북에 적용한다. `1. EDA/2. EDA_practice.ipynb`를 참고하되 실제 데이터와 학습 목표에 맞게 조정한다.

## 대상과 수업 방식

- 대상은 비개발자다. 코드 문법 암기보다 확인할 내용을 자신의 말로 요청하고 표·그래프를 해석하는 경험에 초점을 둔다.
- 설명용 수업에서 배운 분석 방법을 다른 데이터셋에 적용하는 별도 실습으로 구성한다.
- 현재 실습은 Continue extension을 사용한다. 학습자가 코드를 요청하고, 받은 코드를 노트북에 붙여넣어 실행한다.
- 기본 분석은 요청 예시를 보며 함께 진행하고, 이어지는 탐색에서는 학습자가 항목과 요청 문장을 직접 정한다.

## 노트북과 Notion의 역할

- 노트북에는 분석 목적, 데이터 설명, 준비 코드, 단계별 짧은 활동 안내와 실행용 코드 셀을 둔다.
- 복사할 공통 지침과 함께 진행할 분석의 요청 예시는 사용자가 지정한 별도 Notion 페이지에 작성하고 노트북에서 링크한다. 목적지가 없으면 임의의 페이지에 게시하지 않는다.
- 공통 지침은 데이터 변수명, 준비된 도구, 간단한 코드와 설명 요청 등 필요한 맥락만 짧게 담는다. 긴 규칙 목록과 데이터 설명의 중복을 피한다.
- 직접 탐색에는 확인할 내용과 분석 방법을 안내하되 완성된 프롬프트나 정답 코드를 미리 제공하지 않는다.
- 노트북과 Notion의 단계 번호·이름·데이터 변수명·처리 상태를 일치시킨다.

## 준비와 공동 처리

- 시작에 분석 목적이나 핵심 질문, 한 행의 의미, 주요 열의 의미와 단위를 짧게 소개한다.
- 라이브러리·글꼴·데이터 준비 코드는 앞부분에 제공하고 함께 실행한다. 데이터 경로와 불러오기 설명은 실제 코드와 일치시킨다.
- 앞선 설명용 데이터에 없던 결측치 등은 필요한 만큼 소개하고 함께 처리한 뒤 탐색을 시작한다.
- 결측치는 열별 개수와 해당 행을 직접 확인한다. 제외하기·채우기의 차이를 짧게 설명하고 실제 분석 목적에 맞는 방법을 선택한다.
- 원본을 보관하고 처리 전후 행 수를 보여준다. 관련 없는 열의 결측치 때문에 모든 분석에서 행을 일괄 삭제하지 않는다.
- 펭귄 실습에서는 신체 측정값이 빠진 2행을 제외해 342행으로 분석한다. 성별만 빠진 기록은 유지하고 성별 비교 시에만 제외한다. 다른 데이터에 이 수치와 방법을 그대로 적용하지 않는다.

## 단계별 구성

- 짧은 안내 → AI에게 코드 요청 → 실행 → 결과에서 궁금한 내용 선택 → 직접 추가 요청으로 이어지게 한다.
- **함께 진행**과 **직접 탐색**을 표시하고 각 활동 뒤에 실행용 코드 셀을 둔다. 준비·공동 처리 외의 분석 코드를 모두 미리 채우지 않는다.
- 단계 사이에 연결을 만든다. 예를 들어 기초 통계에서 관심 항목을 골라 히스토그램으로 확인하고, 히트맵에서 두 항목을 골라 산점도로 확인한다.
- 산점도를 종별 색으로 나누거나 전체·종별 통계를 비교하는 식으로 이미 본 결과를 다른 관점에서 확인한다.
- 축·색·점의 의미와 필요한 개념은 쉬운 말로 설명한다. 불필요한 전문 용어와 심화 기법을 늘리지 않는다.
- 자유 탐색은 열린 안내만으로 끝내지 않는다. 실제 데이터로 확인 가능한 선택 주제 약 3개를 제시하고 하나를 고르게 한다. 각 주제에 확인할 내용과 적합한 표·그래프를 안내한다.
- 펭귄의 선택 활동 예시는 같은 종 안의 성별 체중 비교, 전체·종별 상관계수 비교, 섬별 종의 개수·비율 비교다. 다른 데이터에서는 구조와 목표에 맞게 바꾼다.
- 반복적인 질문지, ‘내 답’, 답변 작성란은 넣지 않는다. 결과 공유 시에는 선택한 그래프와 추가 확인 결과를 짧게 설명하게 한다.

## 코드와 해석

- 제공 코드는 위에서부터 실행 가능하고 간결해야 한다. 항목 변경용 코드를 제공한다면 `x`, `y` 등으로 열을 지정하고 축·제목도 함께 바뀌게 한다.
- 실제 데이터에 없는 결측치·이상값을 임의로 만들지 않는다. 별도 요청 시 원본과 명확히 구분한다.
- 동일한 행이나 큰 값·작은 값을 곧바로 오류로 단정하거나 자동 삭제하지 않는다.
- AI에게 해석을 요청할 때는 실제 표·그래프를, 오류 해결을 요청할 때는 코드와 오류 메시지를 함께 전달하게 한다. AI가 화면의 출력을 자동으로 안다고 가정하지 않는다.
- 관찰과 추론을 구분한다. 상관관계를 원인으로, 시각적 분리를 검증된 예측 성능으로, 표본의 구성 비율을 전체 모집단의 비율로 단정하지 않는다.

## 수정과 확인

- 사용자가 수정한 현재 파일과 Notion 페이지를 먼저 읽고 요청한 부분을 수정한다. 삭제한 내용이나 사용자가 작성한 코드를 임의로 복원·교체하지 않는다.
- 검토 요청만 받은 경우 파일이나 페이지를 임의로 수정하지 않는다.
- 코드 변경 시 제공된 코드 셀을 가능하면 순서대로 실행하고 처리 결과와 설명을 확인한다. 검증용 정답 코드를 실습 셀에 남기지 않는다.
- 설명만 수정했다면 구성·링크·단계 번호와 기존 코드 보존을 확인한다. 불필요하게 전체 분석을 재실행하지 않는다.
- 제공 코드의 실행 검증, Jupyter 전체 실행, 아직 생성되지 않은 학습자 AI 코드의 미검증을 구분한다. 확인하지 못한 사항을 실행 완료로 표현하지 않는다.
