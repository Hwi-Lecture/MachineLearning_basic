# AI 활용 실습 노트북 작성 원칙

[AGENTS.md](AGENTS.md)의 공통 원칙에 더해 적용한다. 학습자는 Continue에 코드를 요청하고 노트북에서 실행한다.

## 학습 흐름

- 처음엔 준비·기본 분석·결과 읽는 방법까지 탐색의 출발점을 제공한다. 이후에는 관심 결과·분석 항목·변경 방법을 학습자가 스스로 선택한다. 필요하면 결과 비교를 다시 함께 진행할 수 있다.
- 관찰 → 가설 → 시도 → 결과 확인으로 연결한다. 정해진 분석의 빈 코드만 채우는 과제로 만들지 않는다.
- 성능 향상보다 선택 이유와 결과 해석을 중요하게 본다. 구현 방법이나 전문 용어를 스스로 떠올릴 필요는 없다.
- 설명용에서 배운 방법을 새로운 질문에 적용한다. 데이터는 학습 목표와 다른 단원의 사용 계획을 고려해 정하며, 특정 데이터의 처리 방식·활동을 그대로 복제하지 않는다.

## 준비와 활동 안내

- 데이터·도구 준비와 필요한 공동 처리 코드는 제공한다. 결측치가 있으면 해당 항목·행을 확인하고 처리 이유를 짧게 설명한다. 원본과 처리 전후 변화를 보존하며, 관련 없는 결측치 때문에 기록을 일괄 삭제하지 않는다.
- 함께 진행과 직접 탐색을 표시하고 실행용 셀을 둔다. 학생이 요청할 분석 코드는 미리 채우지 않는다.
- 관찰에서 다음 선택으로 이어지게 한다. 예를 들어 예측 산점도에서 관심 오차를 고르고, 해당 기록의 정보표에서 분석할 항목을 찾게 할 수 있다.
- ‘알아서 해보세요’로 끝내지 않는다. 실제 데이터로 확인할 수 있는 접근 방향 몇 가지와 적합한 표·그래프를 안내하되, 학생 자신의 질문도 허용한다.
- 가이드는 질문에 맞는 방법 선택을 돕는다. 산점도·그룹별 비교나 구간·유무·비율 등의 방법을 짧게 안내할 수 있지만, 특정 항목과 변환을 정답으로 연결하지 않는다.
- 힌트는 막힐 때 단계적으로 제공한다. 검증된 성공 방법과 예상 수치는 별도 강사용 자료에 두며 전원의 필수 경로로 만들지 않는다.
- 결과 공유는 선택한 표·그래프로 관찰, 선택 이유, 실제 결과를 짧게 설명하게 한다.

## 노트북과 Notion

- 노트북은 생각하고 실행할 흐름을, Notion은 함께 시작할 짧은 요청 예시를 맡는다. 같은 설명과 활동 전체를 양쪽에 반복하지 않는다.
- 요청 예시는 사용자가 지정한 Notion 페이지에 작성하고 노트북에서 연결한다. 목적지가 없으면 임의로 게시하지 않는다. 양쪽의 단계·변수명·처리 상태를 맞춘다.
- 공통 지침은 짧은 한 단락, 단계별 요청은 보통 1~3문장으로 쓴다. ‘무엇을 보고 싶은지’가 읽혀야 하며 긴 내용을 한 문장에 몰아넣지 않는다.
- 준비 코드를 맥락으로 전달하고, 요청문에는 단계 연결에 꼭 필요한 변수명만 남긴다. 인덱스·자료형·함수 옵션·반복 계산식 등 구현 명세를 나열하지 않는다.
- 직접 탐색에는 완성된 요청문이나 정답 코드를 제공하지 않는다. AI에게 해석을 물을 때는 실제 결과를, 오류를 물을 때는 실행한 코드와 오류 메시지를 함께 전달하게 한다.

## 실험과 확인

- 오차가 큰 기록의 공통점을 찾아 가설을 세운다. 공통점만으로 원인이나 기록 오류를 확정하지 않는다.
- 특성 변경은 모델·정답 단위·평가 기록을 유지하고 한 번에 한 가지를 비교한다. 데이터로 정하는 기준은 훈련 자료에서 구하고 실제 정답을 입력에 넣지 않는다.
- 전체 지표와 처음 관심을 둔 같은 기록·그룹을 함께 비교한다. 평가 기록을 제외하거나 잘 맞는 기록만 다시 골라 개선으로 보이게 하지 않는다.
- 검증용 정답 코드를 학생용 셀에 남기지 않는다. 제공 코드의 실행, Jupyter 전체 실행, 아직 생성되지 않은 학습자 AI 코드의 미검증을 구분한다.
