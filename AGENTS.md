# 수업 자료 작성 지침

대상은 비개발자다. 코드 문법 암기보다 결과를 읽고, 확인할 내용과 변경 이유를 자신의 말로 설명하는 경험에 초점을 둔다.

## 자료 유형

노트북을 작성·수정·검토하기 전에 해당 지침을 읽는다. 경로는 프로젝트 루트 기준이다.

- **설명용(함께하기·시연):** 제공된 코드로 분석 흐름과 결과 읽는 방법을 배운다. [follow_notebook.md](follow_notebook.md)를 적용한다.
- **AI 활용 실습:** 기본 분석 이후 학습자가 관심 결과·분석 항목·방법을 선택한다. [practice_notebook.md](practice_notebook.md)를 적용하며, 연결된 Notion 요청 예시에도 동일하게 적용한다.
- 두 유형을 함께 다루면 두 문서를 모두 읽는다. 준비 코드가 있다는 이유만으로 실습을 설명용으로 분류하지 않는다.

## 공통 원칙

- 현재 파일·페이지와 사용자의 수정 사항을 기준으로 작업한다. 삭제한 내용은 임의로 복원하지 않는다. 검토만 요청받으면 수정하지 않는다.
- 목적, 한 행의 의미, 주요 항목의 단위를 짧게 소개한다. 축·색·점의 의미를 설명하고 불필요한 전문 용어·심화 내용·반복 답변란은 줄인다.
- 실제 데이터에서 확인한 현상을 다룬다. 가상 결측치·이상값을 임의로 끼워 넣지 않으며, 별도 사례가 필요하면 원본과 구분한다. 큰 값이나 중복 기록을 곧바로 오류로 단정하지 않는다.
- 관찰과 추론을 구분한다. 상관관계를 원인으로, 시각적인 구분을 검증된 예측 성능으로 설명하지 않는다.
- 제공 코드는 간결하고 순서대로 실행 가능하게 한다. 설정은 앞에 모으고, 항목을 바꾸면 그래프의 축·제목도 맞게 바뀌도록 한다.
- 코드 변경 후에는 가능한 범위에서 순차 실행과 결과를 확인한다. 설명만 바꾸면 구성·링크·단계 번호와 코드 보존을 확인한다. 실행한 범위와 미검증 사항을 구분해 보고한다.

## 분석 흐름과 학습자의 선택권

**학생이 생각할 부분은 남기고, 실행에 막히는 부분을 돕는다.** 친절한 안내를 이유로 학생의 질문·분석 대상·개선 방법까지 대신 정하지 않는다.

- 설명용은 납득할 수 있는 분석 흐름을 보여준다. 오류의 특징을 발견했다고 익숙한 개선 기법으로 바로 넘어가지 않는다. 학습 자료에서 정답과의 관계를 확인하고, 현재 입력이 그 정보를 어떻게 담고 있는지 살핀 뒤 변경 이유를 설명한다. 분류에서는 같은 실제 라벨 안의 오류 비교와, 두 라벨을 모두 포함한 학습 자료의 관계 확인을 구분한다.
- 임계값 조절은 판단 기준을 바꾸는 실험이다. 특정 그룹에서 틀리는 이유를 분석하거나 입력·학습 방식을 개선하는 과정을 대신하지 않는다.
- 제공할 개선 예시는 실제로 실행한 결과를 바탕으로 쓴다. 흔히 쓰는 방법이라고 개선을 보장하지 않고, 한 번 효과가 없었다고 그 방법 전체가 효과 없다고 일반화하지 않는다. 성공을 만들려고 결과에 맞춰 이유를 끼워 넣지 않는다.
- 설명의 중심은 **무엇을 봤나 → 왜 바꿨나 → 결과가 어땠나**다. 표·지표·조건을 많이 붙이는 것으로 쉬운 설명을 대신하지 않는다.
- AI 활용 실습은 설명용과 겹치는 준비·기본 모델·결과 확인을 앞의 제공 코드로 묶는다. 이후에는 **궁금한 예측 → 분석과 변경 → 결과 확인** 같은 짧은 질문과 빈 실행 셀을 두고, 학생이 AI와 대화하며 직접 진행하게 한다. 구체적인 방법은 막힐 때 펼쳐 보는 힌트로 분리한다.
- 실습용 Notion에는 AI에게 **처음 한 번 전달할 지침만** 둔다. 준비 상태·주요 변수·간단한 코드와 쉬운 설명 요청·학습자가 개선 방법을 먼저 생각한다는 역할을 짧게 전달한다. 단계별 완성 요청문이나 노트북의 활동 안내를 반복하지 않는다. 실험에 필요한 비교 조건은 노트북의 해당 위치에 짧게 둔다.
- 흐름을 수정할 때는 노트북과 연결된 Notion을 함께 점검한다. 사용자가 같은 방향을 다시 설명하거나 남은 불일치를 찾아내게 하지 않는다.

이 절은 AI 활용 실습의 기본 구성과 Notion 역할에 관해 `practice_notebook.md`보다 우선한다. 사용자가 별도로 지정한 형식이 있으면 그 요청을 따른다.
