# 수업 자료 작성 원칙

이 문서는 이 프로젝트 전체의 수업 자료, 특히 Jupyter 노트북(`.ipynb`) 작성·수정에 적용한다.

## 대상과 수업 방식

- 대상은 비개발자이며, 코드 문법 암기보다 코드를 실행하고 표·그래프의 결과를 이해하는 데 초점을 둔다.
- 설명용 노트북에서는 강사와 함께 제공된 코드를 실행하며 분석 흐름과 결과를 살펴본다.
- 학습자가 스스로 탐색하는 활동은 다른 데이터셋을 사용하는 별도 실습으로 구분한다. 설명용 노트북을 반복적인 질문지나 답안 작성 양식으로 만들지 않는다.
- 현재 `1. ML_파이프라인/0. data_EDA.ipynb`의 간결한 설명과 실행 중심 구성을 참고한다. 참고 파일의 문구나 구현상 불일치를 그대로 복제하지는 않는다.

## 설명과 구성

- 시작에 분석 목적이나 핵심 질문, 데이터의 한 행과 주요 항목의 의미·단위를 짧게 설명한다.
- 각 단계는 짧은 설명 → 실행 코드 → 필요한 결과 해석 순서로 구성한다. 결과만으로 분명한 내용은 반복해서 설명하지 않는다.
- 불필요한 질문, 반복적인 토론, ‘내 답’, ‘여기에 적어보세요’ 등의 답변 공간을 넣지 않는다. 질문이 필요하다면 핵심 관찰을 돕는 소수의 구두 질문으로 제한한다.
- 실제 데이터에서 확인할 수 있는 현상에 집중한다. 이론을 모두 다루기 위해 원본에 없는 0·결측치·이상값 등을 가상 사례로 끼워 넣지 않는다. 별도 요청이 있거나 실습 목적상 필요한 경우에는 원본과 명확히 구분한다.
- 현재 학습 목표에 필요하지 않은 심화 기법이나 전문 용어를 늘리지 않는다. 필요하면 별도 후속 자료로 분리한다.
- 축·색·점의 의미와 결과 해석에 필요한 개념을 쉬운 말로 설명한다. 설정이나 내부 구현에 대한 긴 설명은 피한다.
- 도입의 진행 순서, 절 번호, 선택 실습 안내 등은 실제 노트북 구성과 일치시킨다.

## 코드와 시각화

- 위에서부터 순서대로 실행할 수 있는 간결한 코드를 제공한다.
- 필요한 라이브러리·글꼴·데이터 준비 코드는 앞부분에 모으고, 설정 코드의 세부 이해를 요구하지 않는다.
- 비교할 항목을 바꾸는 실습은 `x`, `y` 같은 명확한 변수에 열 이름을 지정하는 방식으로 쉽게 조작할 수 있게 한다. 필요하면 선택 가능한 열 이름을 짧은 주석으로 제공한다.
- 선택 항목이 바뀌면 그래프의 축 이름과 제목도 함께 바뀌도록 한다. 설명과 실제 표시되는 데이터가 일치해야 한다.
- 비슷한 그래프를 불필요하게 반복하기보다 기존 코드의 항목을 바꾸어 비교할 수 있게 한다.
- 실제 데이터의 구조, 결측치, 중복 등 목적에 맞는 점검 결과를 직접 보여준다. 동일한 값의 행을 곧바로 오류로 단정하거나, 통계적 이상치 후보를 자동 삭제하지 않는다.
- 관찰과 추론을 구분한다. 시각적 분리를 검증된 예측 성능으로 표현하거나 상관관계를 인과관계로 설명하지 않는다.

## 수정과 확인

- 사용자가 직접 수정한 현재 파일을 기준으로 작업하며, 삭제한 질문·답변란·심화 내용을 임의로 복원하지 않는다.
- 검토 요청에서는 먼저 현재 상태와 변경점을 확인한다. 수정 요청이 없는 경우 파일을 임의로 변경하지 않는다.
- 코드 수정 후에는 가능하면 전체 셀을 순서대로 실행해 오류를 확인한다. 변수로 항목을 바꿀 수 있는 그래프는 축·제목이 선택 항목과 일치하는지도 확인한다.
- 실행 검증 여부와 확인하지 못한 사항은 결과 보고에서 명확히 구분한다.
