# 수업 자료 작성 지침

대상은 비개발자다. 코드 문법 암기보다 결과를 읽고, 확인할 내용과 변경 이유를 자신의 말로 설명하는 경험에 초점을 둔다.

## 자료 유형

노트북을 작성·수정·검토하기 전에 해당 지침을 읽는다. 경로는 프로젝트 루트 기준이다.

- **설명용(함께하기·시연):** 제공된 코드로 분석 흐름과 결과 읽는 방법을 배운다. [follow_notebook.md](follow_notebook.md)를 적용한다.
- **AI 활용 실습:** 기본 분석 이후 학습자가 관심 결과·분석 항목·방법을 선택한다. [practice_notebook.md](practice_notebook.md)를 적용하며, 연결된 Notion 요청 예시에도 동일하게 적용한다.
- 두 유형을 함께 다루면 두 문서를 모두 읽는다. 준비 코드가 있다는 이유만으로 실습을 설명용으로 분류하지 않는다.

## 공통 원칙

- 현재 파일·페이지와 사용자의 수정 사항을 기준으로 작업한다. 삭제한 내용은 임의로 복원하지 않는다. 검토만 요청받으면 수정하지 않는다.
- 목적, 한 행의 의미, 주요 항목의 단위를 짧게 소개한다. 축·색·점의 의미를 설명하고 불필요한 전문 용어·심화 내용·반복 답변란은 줄인다.
- 실제 데이터에서 확인한 현상을 다룬다. 가상 결측치·이상값을 임의로 끼워 넣지 않으며, 별도 사례가 필요하면 원본과 구분한다. 큰 값이나 중복 기록을 곧바로 오류로 단정하지 않는다.
- 관찰과 추론을 구분한다. 상관관계를 원인으로, 시각적인 구분을 검증된 예측 성능으로 설명하지 않는다.
- 제공 코드는 간결하고 순서대로 실행 가능하게 한다. 설정은 앞에 모으고, 항목을 바꾸면 그래프의 축·제목도 맞게 바뀌도록 한다.
- 코드 변경 후에는 가능한 범위에서 순차 실행과 결과를 확인한다. 설명만 바꾸면 구성·링크·단계 번호와 코드 보존을 확인한다. 실행한 범위와 미검증 사항을 구분해 보고한다.
